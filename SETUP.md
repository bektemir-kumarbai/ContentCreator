# 🔧 Детальная инструкция по настройке

## Получение API ключей

### 1. Google Gemini API

1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите в свой Google аккаунт
3. Нажмите "Get API Key"
4. Скопируйте ключ и добавьте в `.env`:
   ```
   GEMINI_API_KEY=your_key_here
   ```

### 2. ElevenLabs API

1. Зарегистрируйтесь на [ElevenLabs](https://elevenlabs.io/)
2. Перейдите в Profile → API Keys
3. Создайте новый API ключ
4. Скопируйте ключ и добавьте в `.env`:
   ```
   ELEVENLABS_API_KEY=your_key_here
   ```

### 3. ElevenLabs Voice ID

1. В ElevenLabs перейдите в раздел "Voices"
2. Выберите голос для озвучки (или создайте свой)
3. Скопируйте Voice ID (находится под именем голоса)
4. Добавьте в `.env`:
   ```
   ELEVENLABS_VOICE_ID=your_voice_id_here
   ```

## Установка FFmpeg

### Linux (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install ffmpeg
```

### macOS

```bash
brew install ffmpeg
```

### Windows

1. Скачайте FFmpeg с [официального сайта](https://ffmpeg.org/download.html)
2. Распакуйте архив
3. Добавьте путь к `bin` в PATH

## Проверка установки

```bash
# Проверка Python
python --version  # должно быть 3.10+

# Проверка Node.js
node --version  # должно быть 18+

# Проверка Docker
docker --version
docker-compose --version

# Проверка FFmpeg
ffmpeg -version
```

## Первый запуск

### 1. Создайте .env файл

```bash
cp .env.example .env
nano .env  # или используйте любой текстовый редактор
```

### 2. Запустите систему

#### Linux/Mac:

```bash
chmod +x start.sh
./start.sh
```

#### Windows:

```bash
# Запустите PostgreSQL
docker-compose up -d

# В одном терминале - Backend
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python main.py

# В другом терминале - Frontend
cd frontend
npm install
npm run dev
```

### 3. Откройте браузер

Перейдите на `http://localhost:3000`

## Решение проблем

### PostgreSQL не запускается

```bash
# Проверьте, не занят ли порт 5111
lsof -i :5111

# Остановите контейнер и попробуйте снова
docker-compose down
docker-compose up -d
```

### Backend не запускается

```bash
# Проверьте логи
cd backend
source venv/bin/activate
python main.py

# Проверьте подключение к БД
psql -h localhost -p 5111 -U admin -d contentcreator
```

### Frontend не запускается

```bash
# Очистите кэш и переустановите зависимости
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Ошибки при обработке видео

Убедитесь, что FFmpeg установлен:

```bash
ffmpeg -version
```

Если FFmpeg не найден, установите его (см. выше).

## Тестирование системы

### 1. Создайте тестовую притчу

Используйте этот текст для теста:

**Заголовок:** Мудрый дракон

**Текст:**
```
В древней земле жил мудрый дракон по имени Зефирос. 
Он был не таким, как другие драконы - не сжигал деревни и не копил золото.
Зефирос учил людей мудрости и доброте.
Однажды к нему пришёл молодой воин и спросил: "Как стать сильным?"
Дракон ответил: "Истинная сила - в доброте и мудрости, а не в мече."
```

### 2. Запустите обработку

Нажмите "Запустить обработку" и дождитесь завершения.

### 3. Проверьте результаты

- Текст для озвучки должен быть переписан с эмоциональными тегами
- Должны сгенерироваться 3-7 изображений
- Должно создаться аудио

## Рекомендации по использованию

### Для лучших результатов:

1. **Текст притчи**: 150-300 слов (для видео до 60 секунд)
2. **Стиль**: Используйте образный язык, метафоры
3. **Структура**: Начало → развитие → мораль
4. **Язык**: Система поддерживает любой язык

### Работа с Grok:

1. Скачайте изображения из системы
2. Загрузите в Grok с промптом: "Create a short video animation from this image"
3. Скачайте полученные видео
4. Загрузите обратно в систему в правильном порядке

## Мониторинг

### Логи Backend:

```bash
cd backend
source venv/bin/activate
python main.py
# Логи будут отображаться в консоли
```

### Логи PostgreSQL:

```bash
docker-compose logs -f postgres
```

### Проверка статуса:

```bash
# Backend
curl http://localhost:8000

# PostgreSQL
docker-compose ps
```

## Backup базы данных

```bash
# Создать backup
docker exec contentcreator_postgres pg_dump -U admin contentcreator > backup.sql

# Восстановить из backup
docker exec -i contentcreator_postgres psql -U admin contentcreator < backup.sql
```

