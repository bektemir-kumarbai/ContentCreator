# 🎬 Content Creator - YouTube Shorts Generator

Полуавтоматическая система создания YouTube Shorts на основе притч с использованием AI.

## 🌟 Возможности

- ✍️ Переписывание текста притч для озвучки с эмоциональными тегами
- 🎨 Генерация связанных изображений для визуального сторителлинга
- 🎙️ Автоматическая озвучка через ElevenLabs
- 🎥 Автоматическая сборка финального видео
- 📝 Умная генерация субтитров с синхронизацией
- ⏱️ Автоматическая подгонка длительности до 60 секунд
- 🌐 Современный веб-интерфейс

## 🏗️ Архитектура

### Backend (FastAPI)
- PostgreSQL база данных
- Интеграция с Google Gemini API
- Интеграция с ElevenLabs API
- Обработка видео через MoviePy

### Frontend (React + Vite)
- Современный UI/UX
- Загрузка видеофрагментов
- Просмотр результатов

## 📋 Требования

- Docker и Docker Compose
- Python 3.10+
- Node.js 18+
- FFmpeg (для обработки видео)

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd ContentCreator
```

### 2. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Заполните API ключи:

```env
# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key_here

# ElevenLabs API
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
ELEVENLABS_VOICE_ID=your_voice_id_here
```

### 3. Запустите PostgreSQL

```bash
docker-compose up -d
```

### 4. Установите зависимости Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

### 5. Установите зависимости Frontend

```bash
cd frontend
npm install
```

## 🎯 Запуск

### Backend

```bash
cd backend
source venv/bin/activate
python main.py
```

Backend будет доступен на `http://localhost:8000`

### Frontend

```bash
cd frontend
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

## 📖 Использование

### 1. Создание притчи

1. Откройте веб-интерфейс
2. Нажмите "Создать новую притчу"
3. Введите заголовок и текст притчи
4. Нажмите "Создать"

### 2. Обработка притчи

1. Откройте созданную притчу
2. Нажмите "Запустить обработку"
3. Система автоматически:
   - Переписывает текст для озвучки
   - Генерирует метаданные для YouTube
   - Создаёт промпты для изображений
   - Генерирует изображения
   - Создаёт аудио озвучку

### 3. Загрузка видеофрагментов

1. После обработки скачайте сгенерированные изображения
2. Отправьте каждое изображение в Grok для создания видео
3. Загрузите полученные видео обратно в систему
4. Нажмите "Сгенерировать финальное видео"

### 4. Получение результата

Система автоматически:
- Объединит все видеофрагменты
- Синхронизирует с аудио
- Добавит субтитры
- Подгонит длительность до 60 секунд
- Создаст готовое видео для YouTube Shorts

## 🔧 API Endpoints

### Притчи

- `GET /parables` - Список всех притч
- `GET /parables/{id}` - Детальная информация о притче
- `POST /parables` - Создать новую притчу
- `POST /parables/{id}/process` - Запустить обработку
- `DELETE /parables/{id}` - Удалить притчу

### Видео

- `POST /parables/{id}/videos/upload` - Загрузить видеофрагмент
- `POST /parables/{id}/generate-final` - Сгенерировать финальное видео

## 📁 Структура проекта

```
ContentCreator/
├── backend/
│   ├── services/
│   │   ├── gemini_service.py      # Работа с Gemini API
│   │   ├── elevenlabs_service.py  # Работа с ElevenLabs API
│   │   └── video_service.py       # Обработка видео
│   ├── config.py                  # Конфигурация
│   ├── database.py                # Подключение к БД
│   ├── models.py                  # SQLAlchemy модели
│   ├── schemas.py                 # Pydantic схемы
│   ├── main.py                    # FastAPI приложение
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── ParablesList.jsx
│   │   │   ├── ParableDetail.jsx
│   │   │   └── CreateParable.jsx
│   │   ├── App.jsx
│   │   ├── api.js
│   │   └── main.jsx
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
├── database/
│   └── init.sql                   # Схема БД
├── docker-compose.yml
└── README.md
```

## 🎨 Особенности реализации

### Генерация изображений с контекстом

Система использует Gemini в режиме чата для генерации изображений. Это позволяет:
- Сохранять единый стиль на всех изображениях
- Поддерживать консистентность персонажей
- Создавать логически связанную визуальную историю

### Умная синхронизация видео и аудио

- Если видео длиннее аудио → видео ускоряется
- Если видео короче аудио → видео замедляется
- Финальное видео всегда синхронизировано с озвучкой

### Автоматические субтитры

- Разбивка текста на удобные для чтения фрагменты
- Синхронизация с аудио
- Стильное отображение внизу экрана

## 🛠️ Технологии

- **Backend**: FastAPI, SQLAlchemy, PostgreSQL
- **Frontend**: React, Vite, Axios
- **AI**: Google Gemini API, ElevenLabs API
- **Video**: MoviePy, FFmpeg
- **Database**: PostgreSQL
- **Containerization**: Docker

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Создано как полноценная система для автоматизации создания YouTube Shorts контента.

